import 'package:flutter/material.dart';

class Palette {
  static const Color textColor = Colors.white;
  static const Color background = Color(0xFF202231); //Mostly for currently selected Elements and headers...
  static const Color accent = Color(0xFF0AA77E); //Mostly for currently not selected Elements
}